<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.menuplanner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MenuPlanner">
        <activity
            android:name=".Add_Edit_Recipe"
            android:parentActivityName=".RecipeActivity"/>
        <activity
            android:name=".ShoppingList"
            android:label="Shopping List"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".IngredientListActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MenuActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".RecipeActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MenuList"
            android:label="@string/title_activity_menu_list"
            android:theme="@style/Theme.MenuPlanner.NoActionBar" />
        <activity
            android:name=".add_edit_ingredient"
            android:parentActivityName=".IngredientListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>